<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/activity_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"
>

    <org.android.study.ith.view.PassWordEditText
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_margin="20dp"
            android:id="@+id/et_passwd"
    />

    <Button android:layout_width="160dp"
            android:layout_below="@id/et_passwd"
            android:layout_margin="30dp"
            android:layout_height="60dp"
            android:text="@string/app_name"
            android:id="@+id/btn_debounce"
    />

    <org.android.study.ith.view.TouchEventStudyVp android:layout_width="300dp"
            android:layout_height="300dp"
            android:background="@color/colorAccent"
            android:id="@+id/id_touch_vp"
            android:layout_alignParentBottom="true"
            android:gravity="center"
    >

        <org.android.study.ith.view.TouchEventStudyView android:layout_width="200dp"
                android:id="@+id/id_touch_view"
                android:layout_gravity="center"
                android:background="@color/green"
                android:layout_height="250dp"/>

    </org.android.study.ith.view.TouchEventStudyVp>
</RelativeLayout>
